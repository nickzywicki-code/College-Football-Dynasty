/// <reference types="vite/client" />

/** Unique id of this build, injected by vite.config.ts at build time. */
declare const __BUILD_ID__: string;
/** Human-readable build tag shown on the title screen. */
declare const __BUILD_TAG__: string;
